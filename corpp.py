def getPossibleWorlds(defaultsFilename, nameOfPrologFile, nameOfGetPredicate, pathToXSB="xsb", verbose=False):
    if verbose:
        print("Getting Possible Worlds (Running Logical/Probabilistic Reasoner)...")

def runPOMDP(initFilename, pathToPOMDPSolve="pomdp-solve", verbose=False):
    if verbose:
        print("Getting Optimal Policy (Running Probabilistic Planner)...")